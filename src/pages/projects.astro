---
import WindDecorButton from '../components/WindDecorButton.astro';
import BaseLayout from '../layouts/BaseLayout.astro';
import '../styles/projects-page.css';
import { urlFor } from '../lib/sanity';
import { getAllProjects } from '../lib/queries';
import { FALLBACK_PROJECTS } from '../lib/projectFallbacks';

const pageTitle = 'Our Initiatives | Siryus Creative Media';
const pageDescription =
  'Programs and projects we build and run under the Siryus Creative Media umbrella. Artist development, live events and content from Kigali to the world.';

const fromSanity = await getAllProjects();
const projects = fromSanity.length > 0 ? fromSanity : FALLBACK_PROJECTS;
---

<BaseLayout title={pageTitle} description={pageDescription} canonical="https://www.siryushub.com/projects">
  <div class="projects-page">
    <section class="projects-bleed projects-hero projects-reveal" aria-labelledby="projects-hero-heading">
      <div class="projects-hero__ambient" aria-hidden="true">
        <img
          class="projects-hero__ambient-img"
          src="https://images.unsplash.com/photo-1514525253161-7a46d19cd819?w=1920&q=80&auto=format&fit=crop"
          alt=""
          width="1920"
          height="1080"
          loading="eager"
          decoding="async"
        />
        <div class="projects-hero__ambient-scrim"></div>
      </div>
      <div class="projects-hero__mesh" aria-hidden="true"></div>
      <div class="projects-inner">
        <header class="projects-hero__intro">
          <h1 id="projects-hero-heading" class="projects-hero__title">Our Initiatives.</h1>
          <p class="projects-hero__sub">
            These are the programs and projects we build and run under the Siryus Creative Media umbrella. Not client work. Our own.
          </p>
        </header>
      </div>
    </section>

    <section class="projects-inner projects-grid-section" aria-labelledby="projects-grid-heading">
      <h2 id="projects-grid-heading" class="visually-hidden">Siryus initiatives</h2>
      <div class="projects-grid">
        {
          projects.map((project) => {
            const coverUrl = project.coverImage
              ? urlFor(project.coverImage).width(800).height(450).fit('crop').auto('format').url()
              : '';
            const tags = Array.isArray(project.tags) ? project.tags.filter(Boolean) : [];
            const href = `/projects/${project.slug.current}`;
            const cardCopy = project.excerpt?.trim() || project.description;
            return (
              <a class="project-card project-card--surface project-card--link projects-reveal" href={href} data-astro-prefetch>
                {coverUrl ? (
                  <img
                    class="project-card__cover"
                    src={coverUrl}
                    alt={`Cover image for ${project.title}`}
                    width="800"
                    height="450"
                    loading="lazy"
                    decoding="async"
                  />
                ) : null}
                <span class="project-card__cat">{project.category}</span>
                <h3 class="project-card__title">{project.title}</h3>
                <p class="project-card__desc">{cardCopy}</p>
                <div class="project-card__tags" role="list">
                  {tags.map((tag) => (
                    <span class="project-card__tag" role="listitem">
                      {tag}
                    </span>
                  ))}
                </div>
                <div class="project-card__meta">
                  <span>
                    <strong>Year:</strong> {project.year}
                  </span>
                  <span>
                    <strong>Status:</strong> {project.status}
                  </span>
                </div>
                <span class="project-card__learn-more">Learn more</span>
              </a>
            );
          })
        }
      </div>
    </section>

    <section class="projects-bleed projects-cta projects-reveal" aria-labelledby="projects-cta-heading">
      <div class="projects-cta__inner">
        <h2 id="projects-cta-heading" class="projects-cta__title">Have a Project in Mind?</h2>
        <p class="projects-cta__copy">
          Whether it is a release campaign, a brand activation or a creative production, let us talk about what you are building.
        </p>
        <WindDecorButton href="/contact" class="projects-cta__btn">Work With Us</WindDecorButton>
      </div>
    </section>
  </div>

  <script is:inline>
    (function () {
      var reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
      var els = document.querySelectorAll('.projects-reveal');
      if (reduce) {
        els.forEach(function (el) {
          el.classList.add('is-visible');
        });
      } else {
        var io = new IntersectionObserver(
          function (entries) {
            entries.forEach(function (e) {
              if (e.isIntersecting) {
                e.target.classList.add('is-visible');
                io.unobserve(e.target);
              }
            });
          },
          { threshold: 0.1, rootMargin: '0px 0px -24px 0px' }
        );
        els.forEach(function (el) {
          io.observe(el);
        });
      }
    })();
  </script>

  <style is:global>
    .visually-hidden {
      position: absolute;
      width: 1px;
      height: 1px;
      padding: 0;
      margin: -1px;
      overflow: hidden;
      clip: rect(0, 0, 0, 0);
      white-space: nowrap;
      border: 0;
    }
  </style>

  <style>
    .project-card__cover {
      display: block;
      width: 100%;
      height: auto;
      max-height: 14rem;
      object-fit: cover;
      border-radius: var(--radius-md, 12px);
      margin-bottom: 1rem;
    }
  </style>
</BaseLayout>
